# CherryQ Audio Converter v4.0 RC Release Notes

## 版本定位

`v4.0 RC` 是 CherryQ Audio Converter 从“自动转码工具”走向“双工作区音频处理工具”的内部测试版，用于发行审核、回归验证和下一阶段音频编辑前后端整合前的基线确认。

本版本包含：

- 自动转码区稳定收口
- 音频编辑区可用闭环
- 打包命名与发行文档统一
- 已知问题独立记录在 `Known_Issues.md`

## 主要更新

### 自动转码区

- 监听、扫描、拖入三种入队方式统一
- NCM 解码迁移到程序内部 `Temp/NCM` 临时目录
- 支持更多输入格式与更多输出目标格式
- 输出重名自动递增
- 支持本轮“转换到...”临时输出目录
- 转换后支持内嵌歌词写入与外置 `.lrc` 保留

### 音频编辑区

- 支持普通音频导入与拖入
- 内置播放器支持播放、暂停、停止、进度和音量控制
- 支持音频信息读取与写入
- 支持封面读取、导入、移除、恢复和写入
- 支持歌词读取、编辑、另存为 `.lrc`、保存到原 `.lrc`、写入当前音频
- 支持升降调试听与导出为新文件

## 推荐使用流程

1. 第一次使用先设置监听目录和输出目录。
2. 自动转码区用于批量转换、NCM 解码和歌词自动处理。
3. 音频编辑区用于单文件检查、歌词修订、封面维护、元数据写入和升降调处理。

## 自动转码区亮点

- 任务队列支持拖入文件
- 队列支持失败重试和终态清理
- NCM 中间文件不会污染原下载目录
- 输出目录支持按格式自动分流

## 音频编辑区亮点

- 当前文件栏与当前播放源分离显示
- 内嵌歌词优先，不会被同名 `.lrc` 自动覆盖
- 写入前会释放播放器媒体源，降低 `Permission denied` 风险
- 升降调试听不会直接覆盖当前编辑文件

## 已知限制

- 波形图暂未开放
- 自动调式识别暂未开放
- BPM 检测暂未开放
- 不开放覆盖原音频内容
- 某些格式的封面 / 歌词写入兼容性取决于播放器支持
- WAV 的元数据兼容性有限
- 某些机器上的 Qt 播放器格式支持会受到系统环境影响
- 升降调处理速度取决于文件大小和 FFmpeg 后端处理能力

## 后续计划

- v4.1 继续优化 Dashboard、设置页和日志页
- 音频编辑区继续增强调式识别、BPM、波形图和更完整的元数据工作流
- 后续再评估安装器、签名和升级机制
