# CherryQ Audio Converter v5.0 内部测试版 Known Issues

本文件记录 `v5.0 内部测试版` 当前仍未闭合的问题。它用于内部人工验收和代码审查，不代表可对外发布的产品结论。

## 发行前必须继续跟进

- 用户体验未通过：Phase 5.7 的工程链路可用，但页面入口、操作顺序、状态反馈和默认行为仍偏开发/能力验证；单文件转换还要求人工提供完整的新输出文件路径。下一阶段必须先重组“选择 → 配置 → 执行 → 找到结果”的任务流。
- v5.0 内部测试版尚未执行新的 PyInstaller 交付构建和干净 Windows 异机回归；不得将历史 v4.x 打包验证直接当作 v5.0 发布结论。
- 完整 pytest 最近一次出现一个非确定性的 Windows `WinError 32`：Legacy 测试在清理临时 `sample.flac` 时被系统占用，测试立即重跑通过；冻结代码不为该环境锁临时修改。
- 第三方许可证材料待正式发行前补齐：当前 `LICENSES/` 已包含部分可从本机环境提取的许可证文本和外部工具说明，但 `FFmpeg`、`ncmdump`、`PySide6 / Qt` 仍需按最终二进制来源补齐正式许可证材料或官方分发说明。
- QML 与 Legacy Widgets 继续并存。User Trial Mode 是受控人工验收入口，不是最终产品模式；默认 Preview Mode 也不是普通用户工作流。

## 后续版本计划

- 下一阶段先做用户任务流与信息架构，不在 Phase 5.7 冻结后继续堆叠 UI 快捷入口、底层 capability 或自动化行为。
- 图标与视觉资源后续补齐：当前已使用现有图标资源完成打包，后续可继续补齐更完整的品牌图标、文件关联图标和安装包视觉资源。
- 安装器、数字签名和自动更新尚未实现：当前发行形式为 PyInstaller onedir 目录包、`.7z` 压缩包和 7z SFX 自解压包，不是正式安装器。
- 任务状态暂不持久化：任务队列仍以内存状态为主，程序重启后不会恢复上一次的任务队列。

## 当前可接受限制

- 波形图暂未开放。
- 自动调式识别暂未开放。
- BPM 检测暂未开放。
- 不开放直接覆盖原音频内容。
- WAV 的元数据和歌词兼容性有限。
- 某些播放器或系统环境下，QtMultimedia 的解码能力可能有限。
- 升降调处理速度取决于音频文件大小与当前机器性能。
