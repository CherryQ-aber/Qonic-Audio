# CherryQ Audio Converter v3.5.1 测试清单

## 自动化基线

- [x] `python -m unittest discover -v` 全部通过
- [x] 核心 Python 文件通过语法检查
- [x] PyInstaller 构建成功
- [x] 发行目录不包含 `config.json`
- [x] 发行目录包含 `ffmpeg.exe` 与 `ncmdump.exe`
- [x] 发行目录不包含 `ffplay.exe` 与 `ffprobe.exe`
- [x] `.7z` 和自解压 `.exe` 通过 `7z t`

## 首次启动与路径

- [ ] 在无 `config.json` 的发行目录启动程序
- [ ] 监听目录不可用时显示首次使用提示
- [ ] 选择中文、空格路径后可正常保存并监听
- [ ] 默认输出目录位于程序目录下的 `Music_Output`
- [ ] 关闭并重新启动后，用户配置仍可读取

## 监听、扫描与任务列表

- [ ] 新增 MP3、FLAC、WAV、M4A、AAC、OGG 后进入任务列表
- [ ] 扫描已有文件显示进度与结果摘要
- [ ] 重复文件不会重复入列
- [ ] 破损文件转换失败后保留失败状态
- [ ] 失败条目可以重试
- [ ] 正在处理的条目不能被移除或修改目标格式

## NCM

- [ ] 有效 NCM 可解码并转换
- [ ] 假 NCM 显示失败，不生成错误输出
- [ ] NCM 解码产物不会作为独立任务重复入列

## 转换安全

- [ ] 同格式处理后源文件仍存在
- [ ] 输出存在同名文件时旧文件不被覆盖
- [ ] 单文件目标格式优先于全局格式
- [ ] 批量设置与恢复跟随全局可用
- [ ] 转换时不弹出终端窗口
- [ ] 转换进行中退出时不会强制销毁转换线程

## 托盘与发行版

- [ ] 双击托盘图标恢复窗口
- [ ] 托盘菜单可开始/停止监听、扫描和转换
- [ ] 关闭窗口后程序继续在托盘运行
- [ ] 托盘退出后 watcher 不残留
- [ ] 在未安装 Python、FFmpeg 的 Windows 电脑上完成异机测试
