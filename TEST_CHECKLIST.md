# Qonic Audio v5.0 内部测试版测试清单

## 自动化基线

- [x] Phase 5.9.5 默认、自动转码、音频编辑、文件信息、歌词、音频处理、设置和 Legacy 路由 smoke 均通过
- [x] Phase F/F2 文件浏览、队列接线与响应式专项通过
- [x] 布局、主题、日志、可访问性、播放器、检查器与运行模式联合通过
- [x] `python -B -m pytest -q -p no:cacheprovider`：`553 passed, 2 warnings, 70 subtests passed`
- [x] 核心 Python 文件通过语法检查
- [ ] `build_release.ps1` 构建 `Qonic_Audio_v5.0_internal_test`
- [ ] v5.0 发行目录不包含 `config.json`，并包含 `ffmpeg.exe` 与 `ncmdump.exe`
- [ ] v5.0 发行目录不包含 `ffplay.exe` 与 `ffprobe.exe`
- [ ] v5.0 发行目录包含 `README.md`、`CHANGELOG.md`、`Release_Notes_v5.0_Internal_Test.md`、`Known_Issues.md`、`config.example.json`
- [ ] v5.0 `.7z` 和自解压 `.exe` 通过 `7z t`

## QML 模式与安全回归

- [x] 默认启动进入正常运行，转换控制可用；启动本身不扫描、不监听、不转换、不写配置。
- [x] `--preview` 与 `--qml-smoke-test` 保持零真实能力，不扫描、不监听、不转换、不写配置。
- [ ] `QONIC_QML_LIVE=1` 不授予真实 capability。
- [x] `QONIC_QML_USER_TEST=1` 兼容映射到默认用户模式，且所有操作仍需人工点击。
- [x] 默认用户模式、预览模式和兼容入口均拒绝覆盖已有输出、删除或改写源文件，以及未确认的配置写入。
- [ ] 单文件转换只发布到不存在的新目标；目标格式与文件扩展名一致。

## Phase 5.9.4 页面密度与卡片布局

- [x] 自动转码页形成紧凑入口、主任务队列、内联扫描摘要和紧凑操作区；页面自身不再与队列竞争纵向滚动。
- [x] 1080×680、1280×720、1440×900、1536×982、1900×1200、1920×1080 六档真实外壳页面视口均确认任务队列高度不少于可用页面高度的 38%，底部操作可访问且无水平溢出。
- [x] 文件信息页按 1 / 2 / 3 列响应；中宽下技术摘要跨两列下置，宽屏下封面、Metadata、技术信息同排。
- [x] 封面编辑只显示一张当前有效封面，替换、移除、恢复、dirty 和统一导出入口保持可用。
- [x] 歌词页维持歌词预览和草稿工作区的主面积；原始歌词与当前草稿各自的滚动条仍锚定在各自窗格内，不与页面滚动条重合。
- [x] Pitch 参数、试听和导出在足够宽度时三列，窄窗口单列回退；处理流程与结果未改。
- [x] 当前文件摘要和设置草稿头部完成卡片合并；设置分组中宽/宽屏两列、窄屏单列。
- [x] 默认用户模式可见检查覆盖自动转码、文件信息、歌词、Pitch 和设置；QML 页面均可正常显示并关闭。
- [x] Phase 5.9.4 专项、完整 pytest、QML smoke、Legacy imports / Safe Start、compileall 和配置副作用守卫通过。
- [x] QML JavaScript Array 可通过 `QVariantList` 到达全部生产批量入口；右键“本轮转换到……”会调用目录选择，底部“转换到……”会在设置目录后只调度一次选中任务。
- [x] `enqueue_files`、目录扫描、拖入、多选转换、批量重试/移除、格式、参与策略和输出目录 Slot 不再发布为 `PyObject`。
- [ ] 按 `docs/PHASE_5_9_4_PAGE_DENSITY_LAYOUT.md` 使用真实媒体完成最终鼠标级人工验收；不得在验收中覆盖源文件或已有输出。

## Phase 5.9.5-F/F2 全局文件浏览与响应式

- [x] 默认用户模式使用独立唯一 `FolderBrowserModel`；Preview/smoke 保持不可用且不读取真实目录。
- [x] 选中路径使用规范化身份高亮；后台封面小图只应用当前请求并使用有限文件版本缓存。
- [x] TreeView 展开/折叠压力测试保持可见路径唯一；delegate 禁止复用，错误相对目录深度的行不显示、不占高度。
- [x] 文件树拖放使用标准本地文件 URL；AppShell 只在当前主工作区落点调用既有编辑导入或唯一队列入口。
- [x] 文件树拖动状态不依赖瞬时按键轮询提前结束；窗口级提示跨 Pane 保持到显式释放，取消不投放。
- [x] 顶栏运行模式和功能能力摘要黄色框退出生产组件树；全局工作区、文件栏、设置、日志和版本入口保持可达。
- [ ] 人工确认普通音频可分别拖入编辑/转码区，NCM 只能拖入转码区，工作区外释放无动作。
- [x] Qt 原生异步懒加载树只显示目录和受支持音频，目录变更自动刷新但不自动入队。
- [x] 显式根目录、搜索、收藏、最近目录、选择摘要和清理入口已接入。
- [x] 当前根、收藏、最近目录、Pane 显示状态和 220～360 px 宽度可恢复。
- [x] 单击只选择、箭头只展开；双击普通音频只载入播放器且不自动播放。
- [x] NCM 只开放入队；普通音频右键可载入播放器、进入带 dirty guard 的编辑器、加入唯一队列、打开位置和复制路径。
- [x] Pane 可调宽、可从 Pane/顶部收起展开；隐藏时主工作区完整占满，跨工作区保持同一实例。
- [x] 播放器高度 799/800 边界准确。
- [x] 任务检查器 1899×1200、1900×1199、1900×1200 默认展开边界准确。
- [x] Qt 100% / 125% / 150% offscreen 缩放 smoke 通过。
- [ ] Windows 真实 100% / 125% / 150% DPI 人工验收。
- [ ] 按 `docs/PHASE_5_9_5_F_FOLDER_PANE_RESPONSIVE_ACCEPTANCE.md` 完成真实媒体鼠标级验收。
- [ ] 人工确认文件树展开/刷新不卡 UI，双击/右键语义和重启恢复准确。

## Phase 5.9.3 文件信息、封面与歌词职责

- [x] 文件信息页加载 Metadata 表单、文件技术摘要和完整封面草稿编辑。
- [x] 歌词页不加载封面预览、操作、dirty 或封面导出结果。
- [x] 生产可见模块名称统一为“歌词”；兼容键和 QML 文件名保留。
- [x] 进程中只有一个 CoverViewModel，读取结果继续进入同一个 EditSessionViewModel。
- [x] Metadata、Cover、Lyrics dirty 继续由同一会话聚合，文件切换继续使用统一 dirty guard。
- [x] EditExportDialog 仍为唯一正式音频编辑副本导出窗口。
- [x] MP3 / FLAC / M4A / OGG / OPUS 真实组合导出与源 SHA256 守卫通过。
- [x] 可见 QML 结构检查确认导航为“文件信息 / 歌词”，封面编辑只在文件信息页，歌词页无封面 UI。
- [ ] 按 `docs/PHASE_5_9_3_EDITOR_CONTENT_RESPONSIBILITIES.md` 完成鼠标级人工 GUI 验收。

## 首次启动与路径

- [ ] 在无 `config.json` 的发行目录启动程序
- [ ] 监听目录不可用时显示首次使用提示
- [ ] 选择中文、空格路径后可正常保存并监听
- [ ] 默认输出目录位于程序目录下的 `Music_Output`
- [ ] 音频编辑区默认输出目录位于程序目录下的 `AudioEditor_Output`
- [ ] 关闭并重新启动后，用户配置仍可读取
- [ ] `config.json` 损坏时能回退到默认配置
- [ ] `config.json` 缺字段时能与默认配置合并

## 监听、扫描与任务列表

- [ ] 新增 MP3、FLAC、WAV、M4A、AAC、OGG 后进入任务列表
- [ ] 扫描已有文件显示进度与结果摘要
- [ ] 重复文件不会重复入列
- [ ] 破损文件转换失败后保留失败状态
- [ ] 失败条目可以重试
- [ ] 正在处理的条目不能被移除或修改目标格式

## Phase 5.9.2 任务队列控制中心

- [ ] 新任务默认“参与本轮转换”；切换为“本轮跳过”后状态仍保持原生命周期状态
- [ ] 单选、Ctrl 多选、Shift 连选和右键未选中行均作用于正确任务
- [ ] 全局开始只转换“已启用 + 可转换”任务，禁用任务保留在队列
- [ ] “转换此文件”只转换右键任务；“转换选中文件”不影响未选中等待任务
- [ ] 单文件和多选目标格式可覆盖并可恢复跟随全局
- [ ] 单文件和多选本轮输出目录可设置并可恢复默认目录
- [ ] “转换到……”只设置选中任务临时目录并转换选中任务，不修改全局配置
- [ ] 失败重试保留参与、格式和输出目录策略
- [ ] 打开源文件位置和完成后的打开输出位置可用
- [ ] 已有输出保持不变，新输出使用 no-clobber 名称；源文件 SHA256 不变
- [ ] Preview / smoke 不修改任务策略、不转换、不写 `config.json`

## NCM

- [ ] 有效 NCM 可解码并转换
- [ ] 假 NCM 显示失败，不生成错误输出
- [ ] NCM 解码产物不会作为独立任务重复入列
- [ ] 原始下载目录不残留中间 `flac/mp3/wav`

## 转换安全

- [ ] 同格式处理后源文件仍存在
- [ ] 输出存在同名文件时旧文件不被覆盖
- [ ] 单文件目标格式优先于全局格式
- [ ] 批量设置与恢复跟随全局可用
- [ ] 转换时不弹出终端窗口
- [ ] 转换进行中退出时不会强制销毁转换线程
- [ ] 转换后写入歌词失败不会把整条转换误判为失败
- [ ] 开启按格式子文件夹后输出进入 `MP3/FLAC/...` 子目录
- [ ] 关闭按格式子文件夹后输出进入根输出目录
- [ ] 开启“输出时保留外置 .lrc”后输出目录生成同名 `.lrc`

## 音频编辑区

- [ ] 导入 MP3 后可播放、可读信息、可读封面、可读歌词
- [ ] 导入 FLAC 后可读取信息、封面和歌词
- [ ] 导入 M4A 后程序不崩溃，缺失字段显示为空或未知
- [ ] 切换音频时不会保留上一首的封面、歌词和标签
- [ ] 修改标题但不写入时，切换前会提示未保存
- [ ] 写入音频信息后可重新读取，且 dirty 状态清空
- [ ] 导入封面但不写入时只更新当前预览
- [ ] 写入封面后重新导入仍可看到新封面
- [ ] 移除封面并写入后重新导入显示无封面
- [ ] 内嵌歌词优先，不会被同名 `.lrc` 自动覆盖
- [ ] 手动导入 `.lrc` 后来源状态正确
- [ ] 编辑歌词但不写入时，切换前会提示未保存
- [ ] 写入当前音频歌词后 dirty 状态清空
- [ ] 播放时歌词可滚动，无时间轴歌词不会崩溃
- [ ] 升降调试听生成临时文件，当前编辑文件不变
- [ ] 返回当前文件播放后信息、封面、歌词不丢失
- [ ] 升降调导出为新文件但不加载时，原文件不变
- [ ] 升降调导出并加载结果时，当前音频切换到导出结果
- [ ] 升降调导出成功但封面复制失败时显示 warning，不判定导出失败
- [ ] 播放中写入封面 / 歌词 / 元数据时会先释放播放器媒体源

## 托盘与发行版

- [ ] 双击托盘图标恢复窗口
- [ ] 托盘菜单可开始/停止监听、扫描和转换
- [ ] 关闭窗口后程序继续在托盘运行
- [ ] 托盘退出后 watcher 不残留
- [ ] 发行版主窗口标题显示 `Qonic Audio v5.0 Internal Test`
- [ ] 发行版目录名为 `Qonic_Audio_v5.0_internal_test`
- [ ] 发行版主程序名为 `Qonic_Audio_v5.0_internal_test.exe`
- [ ] 在未安装 Python、FFmpeg 的 Windows 电脑上完成异机测试
